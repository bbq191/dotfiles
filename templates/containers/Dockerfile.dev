# 开发环境 Dockerfile 模板
# 针对 Node.js 应用的快速开发配置

FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 安装系统依赖
RUN apk add --no-cache \
    git \
    curl \
    bash \
    postgresql-client \
    redis

# 创建非 root 用户
RUN addgroup -g 1001 -S nodejs && \
    adduser -S nextjs -u 1001

# 复制 package 文件
COPY package*.json ./

# 安装依赖（包括开发依赖）
RUN npm ci --include=dev

# 复制源代码
COPY --chown=nextjs:nodejs . .

# 切换到非 root 用户
USER nextjs

# 暴露端口
EXPOSE 3000

# 开发环境启动命令
CMD ["npm", "run", "dev"]