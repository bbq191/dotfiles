Host *
    ServerAliveInterval 60
    ServerAliveCountMax 3
    ControlMaster auto
    ControlPath ~/.cache/ssh/%C
    ControlPersist 10m

# 旧设备仅支持 ssh-rsa，显式降级以维持连接
Host 40.10.1.53
    HostKeyAlgorithms +ssh-rsa
    PubkeyAcceptedKeyTypes +ssh-rsa
