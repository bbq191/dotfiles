# %h in system units resolves to root's home (not User=), so paths are literal.
[Service]
User=afu
Group=afu
WorkingDirectory=/home/afu/.local/share/ollama
Environment="HOME=/home/afu"
Environment="OLLAMA_MODELS=/home/afu/.local/share/ollama/models"
ProtectHome=no
