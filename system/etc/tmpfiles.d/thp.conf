# Set Transparent Huge Pages to madvise (kernel default is always).
# "always" allocates huge pages eagerly and causes periodic latency spikes
# on desktop workloads; madvise only grants them when explicitly requested.
w /sys/kernel/mm/transparent_hugepage/enabled - - - - madvise
